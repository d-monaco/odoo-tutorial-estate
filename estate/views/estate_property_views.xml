<?xml version="1.0"?>
<odoo>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <record id="estate_view_list" model="ir.ui.view">
        <field name="name">estate.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                decoration-success="state=='offer_received' or state=='offer_accepted'"
                decoration-muted="state=='sold'"
                decoration-bf="state=='offer_accepted'"
                >
                <field name="name" string="Title" width="auto"/>
                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="property_type_id" string="Type" width="auto"/>
                <field name="postcode" string="Postcode" width="auto"/>
                <field name="bedrooms" string="Bedrooms" width="125px"/>
                <field name="living_area" string="Living Area (mq2)" width="150px"/>
                <field name="expected_price" width="auto"/>
                <field name="selling_price" width="auto"/>
                <field name="date_availability" string="Available from" width="auto" optional="true"/>
            </list>
        </field>
    </record>

    <record id="estate_view_form" model="ir.ui.view">
        <field name="name">estate.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Propety">
                <header>
                    <button name="action_sell_property" type="object" string="Sold" invisible="state == 'sold' or state == 'cancelled'"/>
                    <button name="action_cancel_property" type="object" string="Cancel" invisible="state == 'sold' or state == 'cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    
                    <group>
                        <h1>
                            <field name="name" readonly="is_readonly"/>
                        </h1>
                        <field name="property_tag_ids" string="Tags" widget="many2many_tags" options="{'color_field': 'color'}" readonly="is_readonly"/>
                    </group>
                    
                    <group>
                        <group>
                            <field name="property_type_id" can_write="false" options="{'no_create': true}" readonly="is_readonly"/>
                            <field name="postcode" readonly="is_readonly"/>
                            <field name="date_availability" string="Available from" readonly="is_readonly"/>
                        </group>
                        <group>
                            <field name="expected_price" readonly="is_readonly"/>
                            <field name="best_price" readonly="True"/> 
                            <field name="selling_price" readonly="True"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Details">
                            <group>
                                <field name="description" readonly="is_readonly"/>
                                <field name="bedrooms" readonly="is_readonly"/>
                                <field name="living_area" readonly="is_readonly"/>
                                <field name="facades" readonly="is_readonly"/> 
                                <field name="garage" readonly="is_readonly"/>
                                <field name="garden" readonly="is_readonly"/>
                                <field name="garden_area" invisible="not garden" readonly="is_readonly"/>
                                <field name="garden_orientation" invisible="not garden" readonly="is_readonly"/>
                                <field name="total_area" invisible="not garden" readonly="is_readonly"/>
                            </group>
                        </page>
                        
                        <page string="Offers">
                            <list string="Offers">
                                <!-- the list is not editable if the property has an accepted offer, is sold or is cancelled -->
                                <field name="offer_ids" readonly="state == 'offer_accepted' or state == 'sold' or state == 'cancelled'"/>
                            </list>
                        </page>
                        
                        <page string="Other Info">
                            <group>
                                <field name="salesperson_id" readonly="is_readonly"/>
                                <field name="buyer_id" readonly="True"/>
                            </group>
                        </page>
                    </notebook>

                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_search" model="ir.ui.view">
        <field name="name">estate.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Property Search">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="state"/> 
                <field string="Living Area Minimum" name="living_area" filter_domain="[
                    ('living_area','>=',self)
                ]"/>
                <field name="facades"/>
                <filter string="Available" name="available" domain="[
                    '|',
                        ('state','=','new'),
                        ('state','=','offer_received')
                ]"/>
                <group expand="1" string="Group By">
                        <filter string="Postcode" name="postcode" context="{'group_by':'postcode', 'residual_visible':True}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_kanban" model="ir.ui.view">
        <field name="name">estate.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="False">
                <templates>
                    <t t-name="card">
                        <div>
                            <field name="name"/>
                            <field name="state" invisible="True"/><!-- used later for best_price or selling_price -->
                            <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            Expected Price: <field name="expected_price"/>
                            <br/>
                            <div t-if="record.state.raw_value == 'offer_received'">
                                Best Price <field name="best_price"/>
                            </div>
                            <div t-if="record.state.raw_value == 'offer_accepted' or record.state.raw_value == 'sold'">
                                Selling Price <field name="selling_price"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

</odoo>